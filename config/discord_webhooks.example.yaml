# Example only. Do not put real webhook URLs in this file.
webhooks:
  global:
    score_pronos_semaine:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
    predictions_staff:
      webhook_url_env: DISCORD_WEBHOOK_PREDICTIONS_STAFF
      enabled: true
  ligue1:
    predictions:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
    combines:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: false
    analyses:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
  premier_league:
    predictions:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
    resultats:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
  cdm_2026:
    calendrier:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
    predictions:
      webhook_url: "REPLACE_WITH_WEBHOOK_URL"
      enabled: true
